# VBT 系统依赖
# tflite_runtime 与 NumPy 2.x 不兼容，必须固定 numpy<2
numpy>=1.19.0,<2
opencv-python>=4.5.0
matplotlib>=3.3.0
streamlit>=1.20.0
pandas>=1.3.0

# TFLite 推理后端（二选一）
# 树莓派推荐（体积小，启动快）:
#   pip install tflite-runtime
# 其他平台（macOS/Linux x86）:
#   pip install tensorflow>=2.5.0

# 树莓派推理与 CSI 摄像头支持
# tflite-runtime 体积小、启动快；picamera2 用于 Raspberry Pi CSI 摄像头模式。
tflite-runtime
picamera2

# 可选性能监控:
psutil
